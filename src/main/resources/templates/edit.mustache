<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Movie Reviews</title>

    <link rel="stylesheet" type="text/css" href="/css/main.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css">

    <meta name="theme-color" content="#fafafa">
</head>

<body>
<div class="top-bar">
    <h2>Delete Movies</h2>
    <form class="search" action="/dynamic/edit" method="get">
        <input class="bar" type="text" placeholder="search" name="search" />
        <input class="btn" type="submit" value="Enter" />
    </form>
</div>

<div class="movie-box">
    {{#movies}}
        <div class="card movie">
            <b>{{title}}</b>
            <form action="/dynamic/delete:{{movieId}}" method="post">
                <input class="btn" type="submit" value="Delete" />
            </form>
        </div>
    {{/movies}}
</div>
</body>

</html>
